import 'package:flutter/material.dart';

class SocialChats extends StatelessWidget {
  const SocialChats({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Messages')),
    );
  }
}
